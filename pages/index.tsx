import type { NextPage } from 'next';
import SectionCTA from '../components/organisms/SectionCTA/SectionCTA';

const Home: NextPage = () => (
  <div>
    <SectionCTA />
  </div>
);

export default Home;
